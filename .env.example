# Optional non-secret overrides. Defaults work without copying this file.
# Keep ports unique. All published endpoints bind to 127.0.0.1.
SIBASE_DASHBOARD_PORT=58200
SIBASE_API_PORT=58210
SIBASE_PROJECT_PORT_BASE=58201
# Credentials are generated in .local/<stack>/, never in this file.
